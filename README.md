ه# Bash Scripting Challenge Lab

This lab demonstrates a basic Bash script that generates 25 empty files named `hani1` to `hani25`.

## Script: `create_files.sh`

### What it does:
- Loops from 1 to 25
- Creates empty files named `hani1`, `hani2`, ..., `hani25`
- Uses `touch` command inside a loop

### How to run:
```bash
chmod +x create_files.sh
./create_files.sh
# Project 1 - Bash Lab
